import 'package:flutter/material.dart';

void main() {
  runApp(const beDarf());
}

class beDarf extends StatelessWidget {
  const beDarf({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}